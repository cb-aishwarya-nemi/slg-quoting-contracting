# Notes

Workbench:

```
[ ] Sort out Filter and Sort UX.
[ ] Creating Views in all index pages
[x] Left nav finesse and component improvement 
    {Didn't implement the View transitions API since less performant for this usecase}
    {Uses CSS-hover transition}
[ ] Site Switcher
[ ] Global search UX
[x] Drop contract pill in the workbench and Contract Queue
[x] Upload pill only shows on workbench and contract queue pages
```

Customer Link Modal:

```
[x] Switch tab order for no-match variant (Create New Customer first)
[x] Remove stickiness when No match found usecase
```

Contract Ingestion:

```
[ ] In page nav finesse. 
[ ] Gradient touch to CTAs
[ ] Rethink comments UX (Grouping, and annotations for each section)
[ ] Product and pricing -- table improvement in terms of anchor
[ ] Invoices animation for swtiching
[ ] Invoices -- better switcher for viewing future invoices
[ ] Page end flashiness
[ ] Expand contract section than collapsing Comments section
```

Customer 360 Index pages:

```
[ ] Look into the index finesse
```

New Priority:

```
[ ] Do a lean headless version of the contract ingestion Context: @temp-context.md
```

