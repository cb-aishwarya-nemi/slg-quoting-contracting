/**
 * Soft grey pill worn by any field that is in an editing state — the label/value
 * rows and the expanded products table both use it, so the page reads as one set
 * of controls rather than two.
 */
export const ACTIVE_FIELD_STYLE =
  'w-full rounded bg-neutral-100 px-2 py-1 text-[14px] font-medium text-brand-navy outline-none focus:bg-neutral-200'
